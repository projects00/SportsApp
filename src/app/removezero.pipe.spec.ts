import { RemovezeroPipe } from './removezero.pipe';

describe('RemovezeroPipe', () => {
  it('create an instance', () => {
    const pipe = new RemovezeroPipe();
    expect(pipe).toBeTruthy();
  });
});
