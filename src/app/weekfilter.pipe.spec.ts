import { WeekfilterPipe } from './weekfilter.pipe';

describe('WeekfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new WeekfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
